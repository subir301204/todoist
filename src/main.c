/*
 * This is the initial code for the program
 */

#include <stdio.h>

int main(int argc, char *argv[]) {
  if (argc < 2) {
    fprintf(stderr, "\nERROR: Wrong argument count...\n");
  }

  /*
   * There should be a help function to se how can we use the tool
   * I am thinking to implement that without any flag
   * You just have to enter the tools name and it will shouw all the functionalities
   */

  return 0;
}
